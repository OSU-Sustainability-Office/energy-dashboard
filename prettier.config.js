export default {
  printWidth: 120, // matches ESLint's max-len rule
  singleQuote: true,
  trailingComma: 'none',
  arrowParens: 'avoid', // parentheses around arrow function parameters only when necessary
  tabWidth: 2, // matches ESLint's indent rule
  semi: false
}
