{
  "name": "data-collection",
  "version": "1.0.0",
  "description": "This software listens for post requests from acquisuites. When data is received, this server parses through the data and inserts it into the temporary storage in our MySQL database.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jack Woods",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.18.3",
    "dotenv": "^5.0.1",
    "express": "^4.16.3",
    "express-xml-bodyparser": "^0.3.0",
    "mysql": "^2.15.0"
  }
}
